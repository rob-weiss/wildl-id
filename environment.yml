name: wildlife
channels:
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - setuptools<71
      - pandas
      - matplotlib
      - opencv-python
      - python-dateutil
      - torch
      - PytorchWildlife
      - lightning
      - omegaconf
      - pyobjc-framework-Vision; sys_platform == "darwin"
      - pyobjc-framework-Quartz; sys_platform == "darwin"
      - easyocr
      - ephem
      - astral
