name: wildlife
channels:
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas
      - matplotlib
      - opencv-python
      - python-dateutil
      - torch
      - PytorchWildlife
